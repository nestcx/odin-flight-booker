<%= render 'shared/flight_info' %>

<%= form_for(@booking) do |f| %>
	<%= f.hidden_field(:flight_id) %>

	<%= f.fields_for :passengers do |pass| %>
		Name: <%= pass.text_field :name %><br>
		Email: <%= pass.text_field :email %><br><br>
	<% end %>

<%= f.submit %>

<% end %>